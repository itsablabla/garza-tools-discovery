[project]
name = "garza-tools-discovery"
version = "3.1"
requires-python = ">=3.10"
dependencies = ["fastmcp","pyyaml"]

[project.scripts]
garza-tools-discovery = "server:mcp"
garza-discovery-full = "server:mcp"
